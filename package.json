{
  "name": "cpr-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "now-lambda.js",
  "scripts": {
    "deploy": "now alias $(now) cpr-server.now.sh",
    "start": "micro",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "micro": "^9.3.3"
  }
}
